\begin{figure}[t]
\centering
\begin{tikzpicture}[
  font=\footnotesize,
  block/.style={draw, rounded corners=2pt, align=center, minimum height=13mm, text width=25mm, fill=blue!5},
  theory/.style={draw, rounded corners=2pt, align=center, minimum height=13mm, text width=25mm, fill=green!7},
  output/.style={draw, rounded corners=2pt, align=center, minimum height=13mm, text width=25mm, fill=orange!8},
  arrow/.style={-{Latex[length=2mm]}, thick}
]
  \node[theory] (conditions) {Post-tonal conditions\\pc set/IV, row form, rhythm, gesture};
  \node[block, right=5mm of conditions] (encoders) {Condition encoders\\prefix tokens};
  \node[block, right=5mm of encoders] (generator) {Score generator\\causal Transformer};
  \node[theory, right=5mm of generator] (decoder) {Constraint-guided decoder\\$K$ candidates};
  \node[output, right=5mm of decoder] (outputs) {MusicXML score\\+ analysis report};
  \draw[arrow] (conditions) -- (encoders);
  \draw[arrow] (encoders) -- (generator);
  \draw[arrow] (generator) -- (decoder);
  \draw[arrow] (decoder) -- (outputs);
\end{tikzpicture}
\caption{Corrected inference path. Post-tonal conditions are serialized by condition encoders, consumed by the causal score generator, and evaluated by a constraint-guided decoder before MusicXML and an analysis report are exported. A legal procedural generator supplies the condition--score pairs used for training.}
\label{fig:method-pipeline}
\end{figure}
