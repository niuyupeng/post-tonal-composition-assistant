\section{Results}
\label{sec:results}

\subsection{Corrected full-run status}

The corrected v3 corpus split is available with 20,000 training, 2,000 validation, and 2,000 test fragments and \texttt{smoke=false}. The code-level verification suite and CPU smoke workflow pass, and the independent rule reference has been exercised on the full test split. These checks establish pipeline integrity but do not replace neural training.

The corrected neural experiment matrix is not yet complete. Token accuracy, constraint metrics, ablation comparisons, and proposed-versus-vanilla differences therefore remain \texttt{PENDING\_REAL\_}\allowbreak\texttt{EXPERIMENT}. Legacy v2 numerical tables, figures, and controlled-decoding statistics are not used as evidence for the corrected protocol.

\subsection{Formal result tables}

\IfFileExists{tables/project2_v3_main_results.tex}
  {The full runner populated Table~\ref{tab:project2-main-results} directly from the corrected CSV file after all checkpoint and provenance gates passed.
   \input{tables/project2_v3_main_results}}
  {\noindent\texttt{PENDING\_REAL\_EXPERIMENT}: the corrected main-result table has not yet been generated.}

\IfFileExists{tables/project2_v3_ablation_results.tex}
  {The full runner populated Table~\ref{tab:project2-ablation-results} directly from the corrected CSV file after all checkpoint and provenance gates passed.
   \input{tables/project2_v3_ablation_results}}
  {\noindent\texttt{PENDING\_REAL\_EXPERIMENT}: the corrected ablation table has not yet been generated.}

\subsection{MusicXML examples}

Twenty corrected full-model examples and paired analysis reports will be admitted to the expert package only if each file parses as MusicXML and matches the requested measure and voice counts. The final count and automatic metrics remain \texttt{PENDING\_REAL\_EXPERIMENT} until the proposed checkpoint evaluation completes.
